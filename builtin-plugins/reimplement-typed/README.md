# Proof of concept: a plugin that reimplements various typed commands
Note: Currently the plugin is not yet functional, because plugins cannot register custom commands yet.

Compile with `cargo component build`.
The compiled WASM component can then be found at `../target/wasm32-unknown-unknown/debug/hello-world.wasm`.